# Instalación de SRA Toolkit (para prefetch y fasterq-dump)
conda install -c bioconda sra-tools
# o
wget https://ftp-trace.ncbi.nlm.nih.gov/sra/sdk/current/sratoolkit.current-ubuntu64.tar.gz
tar -vxzf sratoolkit.current-ubuntu64.tar.gz
export PATH=$PATH:$PWD/sratoolkit.current-ubuntu64/bin

# Instalación de Kraken2
conda install -c bioconda kraken2

# Instalación de Kaiju
conda install -c bioconda kaiju

# Instalación de HUMAnN
conda install -c bioconda humann

# Instalación de FastQC (control de calidad)
conda install -c bioconda fastqc

# Instalación de MultiQC (resumen de múltiples muestras)
conda install -c bioconda multiqc